# Token de autenticação — gere um novo com: node -e "require('crypto').randomBytes(32).toString('hex')"
BRIDGE_TOKEN=cole_o_token_aqui

# SQL Server local (na máquina do cliente)
DB_HOST=localhost
DB_PORT=1433
DB_NAME=SEMPRETEMDETUDO
DB_USER=lc_dashboard
DB_PASS=REDACTED-SENHA

# Porta da bridge (exposta pelo Cloudflare Tunnel)
PORT=3055
